{% extends 'base.html.twig' %}


{% block title %}Series{% endblock %}


{% block body %}
<script>
window.location.replace(idSerie);
</script>
    <h1>Series</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ series.id }}</td>
            </tr> #}
            <tr>
                <th>Title</th>
                <td>{{ series.title }}</td>
            </tr>
            <tr>
                <th>Plot</th>
                <td>{{ series.plot }}</td>
            </tr>
            {#  <tr>
                <th>Imdb</th>
                <td>{{ series.imdb }}</td>
            </tr> #}
            <tr>
                <th>Poster</th>
                <td><img src="{{ path('poster_series_show', {'id': series.id} ) }}"/></td>
            </tr>
            <tr>
                <th>Director</th>
                <td>{{ series.director }}</td>
            </tr>
            <tr>
                <th>YoutubeTrailer</th>
                <td>{{ series.youtubeTrailer }}</td>
            </tr>
            <tr>
                <th>Awards</th>
                <td>{{ series.awards }}</td>
            </tr>
            <tr>
                <th>YearStart</th>
                <td>{{ series.yearStart }}</td>
            </tr>
            <tr>
                <th>YearEnd</th>
                <td>{{ series.yearEnd }}</td>
            </tr>
            <tr>
                <ul> <th>Seasons</th>
                <td>
                {% for season in series.seasons|sort((a, b) => a.number <=> b.number) %}
                        <li>Saison {{ season.number }} ({{ season.episodes|length }} épisodes)</li>
                {% endfor %}
                </td>
                </ul>
                {% if valeur == 1 %}
                    <input type="checkbox" id="suivi" onchange="changerPage({{series.id}},{{app.user.id}})" checked>
                {% else %}
                    <input type="checkbox" id="suivi" onchange="changerPage({{series.id}},{{app.user.id}})">
                {% endif %}
                
            </tr>
            <tr>
        </tbody>
    </table>

    <a href="{{ path('app_series_index') }}">back to list</a> 

   {# <a href="{{ path('app_series_edit', {'id': series.id}) }}">edit</a> #}

   {#  {{ include('series/_delete_form.html.twig') }}        #}

   <script>
    function changerPage(idSerie, idUser) {
        if (document.getElementById("suivi").checked){
            window.location.replace(idSerie+"/set_following/"+"1");
            {# window.location.replace({{ path('app_series_show_adds', {'series' : series.id, 'yesno' : '1'}) }}); #}
        }
        else {
            window.location.replace(idSerie+"/set_following/"+"0");
            {# window.location.replace({{ path('app_series_show_adds', {'series' : series.id, 'yesno' : '0'}) }}); #}
        }
    }
    </script>


{% endblock %}
